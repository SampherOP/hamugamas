{
  "rules": {
    ".read": false,
    ".write": false,

    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['username','email']) && newData.child('username').isString() && newData.child('username').val().matches(/^[a-zA-Z0-9_-]{3,24}$/) && newData.child('email').isString() && newData.child('email').val() === auth.token.email"
      }
    },

    "publicUsers": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['username','points','streak','playtimeMinutes','achievements','registeredAt','lastSeen','online','playing','currentGame','visits']) && newData.child('username').isString() && newData.child('username').val().length <= 40 && newData.child('points').isNumber() && newData.child('streak').isNumber() && newData.child('playtimeMinutes').isNumber() && newData.child('registeredAt').isNumber() && newData.child('lastSeen').isNumber() && newData.child('online').isBoolean() && newData.child('playing').isBoolean() && newData.child('currentGame').isString() && newData.child('currentGame').val().length <= 80 && newData.child('visits').isNumber() && (!newData.child('gamesPlayed').exists() || newData.child('gamesPlayed').isNumber()) && (!newData.child('uniqueGames').exists() || newData.child('uniqueGames').isNumber()) && (!newData.child('objectiveCount').exists() || newData.child('objectiveCount').isNumber()) && (!newData.child('avatar').exists() || (newData.child('avatar').isString() && newData.child('avatar').val().length <= 200000)) && (!newData.child('usernameChangeUsed').exists() || newData.child('usernameChangeUsed').isBoolean())"
      }
    },

    "usernames": {
      "$username": {
        ".read": true,
        ".write": "auth != null && ((newData.exists() && newData.child('uid').val() === auth.uid) || (!newData.exists() && data.child('uid').val() === auth.uid))",
        ".validate": "!newData.exists() || (newData.hasChildren(['uid','email','username']) && newData.child('uid').isString() && newData.child('email').isString() && newData.child('email').val() === auth.token.email && newData.child('username').isString())"
      }
    },

    "publicChat": {
      ".read": "auth != null",
      "$messageId": {
        ".write": "auth != null && !data.exists() && newData.child('uid').val() === auth.uid",
        ".validate": "newData.hasChildren(['uid','user','text','timestamp']) && newData.child('uid').isString() && newData.child('uid').val() === auth.uid && newData.child('user').isString() && newData.child('user').val().length <= 40 && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 300 && newData.child('timestamp').isNumber() && newData.child('timestamp').val() <= now + 10000"
      }
    },

    "publicChatRate": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['lastSentAt']) && newData.child('lastSentAt').isNumber() && (!data.exists() || newData.child('lastSentAt').val() >= data.child('lastSentAt').val() + 30000)"
      }
    },

    "privateChats": {
      "$chatId": {
        ".read": "auth != null && (data.child('members').child(auth.uid).val() === true || data.child('hostUid').val() === auth.uid)",
        ".write": "auth != null && ((!data.exists() && newData.child('hostUid').val() === auth.uid) || (data.exists() && data.child('hostUid').val() === auth.uid))",
        "members": {
          "$memberUid": {
            ".read": "auth != null && (auth.uid === $memberUid || data.parent().parent().child('members').child(auth.uid).val() === true || data.parent().parent().child('hostUid').val() === auth.uid)",
            ".write": "auth != null && (data.parent().parent().child('hostUid').val() === auth.uid || (auth.uid === $memberUid && root.child('chatInvites').child(auth.uid).child($chatId).child('status').val() === 'pending'))",
            ".validate": "newData.val() === true"
          }
        },
        "messages": {
          ".read": "auth != null && (data.parent().parent().child('members').child(auth.uid).val() === true || data.parent().parent().child('hostUid').val() === auth.uid)",
          "$messageId": {
            ".read": "auth != null && (data.parent().parent().child('members').child(auth.uid).val() === true || data.parent().parent().child('hostUid').val() === auth.uid)",
            ".write": "auth != null && !data.exists() && (data.parent().parent().child('members').child(auth.uid).val() === true || data.parent().parent().child('hostUid').val() === auth.uid) && newData.child('uid').val() === auth.uid",
            ".validate": "newData.hasChildren(['uid','user','text','timestamp']) && newData.child('uid').isString() && newData.child('uid').val() === auth.uid && newData.child('user').isString() && newData.child('user').val().length <= 40 && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 300 && newData.child('timestamp').isNumber() && newData.child('timestamp').val() <= now + 10000"
          }
        }
      }
    },

    "chatInvites": {
      "$targetUid": {
        ".read": "auth != null && auth.uid === $targetUid",
        "$inviteId": {
          ".write": "auth != null && ((auth.uid === $targetUid) || (newData.exists() && newData.child('hostUid').val() === auth.uid))",
          ".validate": "!newData.exists() || (newData.hasChildren(['chatId','hostUid','host','createdAt','status']) && newData.child('chatId').isString() && newData.child('hostUid').isString() && newData.child('hostUid').val() !== $targetUid && newData.child('host').isString() && newData.child('createdAt').isNumber() && (newData.child('status').val() === 'pending' || newData.child('status').val() === 'accepted' || newData.child('status').val() === 'declined'))"
        }
      }
    },

    "votes": {
      "$cycle": {
        ".read": "auth != null",
        "$uid": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid === $uid",
          ".validate": "newData.isString() && newData.val().length <= 100"
        }
      }
    },

    "hostRooms": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "rooms": {
      ".read": "auth != null",
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null && ((!data.exists() && newData.child('hostUid').val() === auth.uid) || (data.exists() && data.child('hostUid').val() === auth.uid))"
      }
    },

    "roomPlayers": {
      "$roomId": {
        ".read": "auth != null",
        "$uid": {
          ".read": "auth != null",
          "$clientId": {
            ".write": "auth != null && $uid === auth.uid && (!data.exists() || data.child('uid').val() === auth.uid)"
          }
        }
      }
    },

    "roomChat": {
      "$roomId": {
        ".read": "auth != null",
        "$messageId": {
          ".write": "auth != null && !data.exists() && newData.child('uid').val() === auth.uid",
          ".validate": "newData.hasChildren(['uid','user','text','timestamp']) && newData.child('uid').val() === auth.uid && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 300 && newData.child('timestamp').isNumber()"
        }
      }
    },

    "roomReactions": {
      "$roomId": {
        ".read": "auth != null",
        "$reaction": {
          "$uid": {
            ".read": "auth != null",
            ".write": "auth != null && $uid === auth.uid"
          }
        }
      }
    }
  }
}
